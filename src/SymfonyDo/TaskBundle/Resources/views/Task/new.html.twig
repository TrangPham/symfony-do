{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Task creation</h1>

    {{ form(form) }}
    <a href="{{ path('task') }}" class="btn btn-block btn-lg btn-default btn-form">
        Cancel
    </a>

{% endblock %}
